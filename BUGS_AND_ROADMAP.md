# BUGS CORRIGÉS

- **[2026-09-04]** `page-about.php`, `page-contact.php` et `page-dons.php` n'avaient pas d'en-tête `Template Name`, rendant invisibles dans l'admin les champs ACF des Tâches 8 et cassant silencieusement le lien "Nous contacter" du footer (voir `DECISIONS.md`).
- **[2026-09-04]** Liens `tel:`/`mailto:` échappés avec `esc_attr()` au lieu de `esc_url()` sur 4 gabarits (8 occurrences) ; 4 liens de partage social de `single.php` renforcés de la même façon (voir `DECISIONS.md`).
- **[2026-09-04]** `template-parts/hero-slider.php` créé en Tâche 1 n'était jamais appelé : le hero de l'accueil était resté codé en dur dans `front-page.php` depuis la Tâche 4. Balisage extrait dans le template-part, appelé via `get_template_part()` (voir `DECISIONS.md`).
- **[2026-09-04]** `menu_position` du CPT `paroisse` (20) entrait en collision avec le menu natif "Pages" de WordPress. Les 4 CPT métier décalés vers des créneaux libres (21–24, voir `DECISIONS.md`).
- **[2026-09-04]** Le formulaire de commentaires (`comments.php`) ne préremplissait pas nom/e-mail/site en cas de réaffichage après une erreur de validation (à la différence du comportement par défaut de `comment_form()`). Ajout de `wp_get_current_commenter()` pour préremplir ces trois champs, avec échappement `esc_attr()`.
- **[2026-09-05]** Sur `archive.php` (pages de catégorie/étiquette), le titre affiché par `template-parts/page-title.php` montrait les balises HTML brutes de `get_the_archive_title()` (ex. `Catégorie : <span>Homélies</span>`) au lieu d'un rendu stylé, car le titre passait dans `esc_html()`. Remplacé par `wp_kses_post()` sur `$dz_title` dans `template-parts/page-title.php` (h1 uniquement) : le balisage sûr renvoyé par `get_the_archive_title()`/`get_the_archive_description()` s'affiche correctement tout en restant assaini contre le HTML dangereux. Le fil d'ariane (`$dz_breadcrumb`) continue de passer par `wp_strip_all_tags()` en amont dans `archive.php`, donc reste en texte brut sans changement.
- **[2026-09-05]** Méga-menu (PROMPT 17) : les nouveaux `<ul>` du panneau (`.mega-menu-columns`, `.mega-menu-sublist`) héritaient de la règle générique `.navmenu ul` — `display:flex`/`align-items:center` en desktop (petits-enfants collés sur une ligne, colonnes mal alignées verticalement), `position:absolute`/fond/ombre en mobile (panneaux flottants superposés au lieu de s'empiler). Trouvé par capture d'écran Chrome headless d'une maquette statique (pas de WordPress réel disponible pour tester, voir DECISIONS.md). Corrigé par une réinitialisation complète de ces propriétés (`display`, `align-items`, `position`, `inset`, `background`, `box-shadow`, `z-index`, `overflow`) plutôt qu'une seule propriété à la fois — voir `DECISIONS.md` "Bug — `.navmenu ul`/`.navmenu li` génériques polluaient les nouvelles listes/colonnes du méga-menu".
- **[2026-09-05]** PROMPT 12 (revue design globale) — `404.php` et `index.php` étaient toujours des stubs `TODO (Phase X): to implement.` (aucun `get_header()`/`get_footer()`, page blanche pour tout visiteur tombant sur une URL invalide, ou pour toute requête retombant sur le gabarit générique de secours). `404.php` réimplémenté en reprenant le balisage exact de `404.html` (icône, code, titre, texte, formulaire de recherche `.error-404 .search-box` — connecté à la vraie recherche WordPress plutôt qu'à l'`action="#"` du template source — bouton retour à l'accueil). `index.php` réimplémenté en gabarit de secours minimal et générique (titre + boucle simple), volontairement sans hypothèse sur un champ/une taxonomie précis puisque chaque type de contenu réel du thème a déjà son propre gabarit dédié et que celui-ci ne sert que de filet de sécurité WordPress.
- **[2026-09-05]** PROMPT 12 (revue design globale) — `page-contact.php` était la seule page/gabarit du thème dont la section principale n'avait aucun `data-aos` (vérifié par comptage sur tous les `single-*.php`/`archive-*.php`/`page-*.php`) ; incohérence héritée du template source (`contact.html` n'a jamais eu de `data-aos` sur `.contact`, contrairement à `about.html`/`starter-page.html`). `data-aos="fade-up" data-aos-delay="100"` ajouté sur le conteneur, pour la même entrée en fondu que toutes les autres pages du site.

---

# IMPORT DE CONTENU — VÉRIFICATIONS (`CONTENT_PROMPTS.md`)

- **[2026-09-07] Recadrage des 2 images "carrées" du hero (PROMPT 1)** — `eveque.jpg`/`benediction_par_eveque.jpg` (1080×810, ratio 1.33) sont nettement moins larges que `messe.jpg`/`consecration.jpg` (1280×575, ratio 2.23), et `.blog-hero-item img { object-fit: cover }` (bannière `height: 80vh, min-height: 600px`) recadre donc plus fort ces deux-là. Vérifié **par rendu réel** (Chrome headless, pas juste en théorie) : le fichier CSS/markup exact du thème appliqué aux 2 images, capturé à 1440×900 (desktop) et 390×844 (mobile). Résultat : le sujet principal (visage/mitre de l'évêque sur `eveque.jpg`, geste de bénédiction sur `benediction_par_eveque.jpg`) reste pleinement visible aux deux gabarits testés avec le recadrage **par défaut** (`object-position: center`, non modifié) — comparé aussi contre `object-position: center top`, qui n'apporte pas d'amélioration notable ici. **Aucun `object-position` spécifique ajouté** : le recadrage par défaut suffit sur les tailles testées. Non vérifié : les largeurs intermédiaires (tablette ~768-991px) et le rendu réel dans le slider Swiper (transitions, éventuel zoom Ken Burns) — à confirmer visuellement après déploiement sur le vrai site (voir point suivant), en particulier si le diocèse fournit un recadrage manuel différent de celui du photographe.
- **[2026-09-07] Vérification finale du hero après déploiement (PROMPT 1, point 5)** — Aucun environnement WordPress réel dans ce dépôt (contrainte récurrente, voir `DECISIONS.md`) : impossible de charger `front-page.php` avec Swiper/AOS en conditions réelles. La logique de `dz_import_run_hero()` (idempotence, gestion d'erreur de sideload, image introuvable) a été vérifiée par un test PHP autonome simulant les fonctions WordPress/ACF (non versionné, script de session) — 11 assertions passées, y compris sur les 4 vrais fichiers image de `assets/seed-images/hero/`. **Reste à faire après déploiement réel** : cliquer "Importer le hero" dans Réglages > Import contenu diocèse, vérifier que les 4 slides apparaissent dans le slider avec le bon recadrage à toutes les tailles d'écran, et que les 4 images sont bien de vrais attachments dans la médiathèque (pas de simple copie de fichier).
- **[2026-09-07] `CALENDRIER_DIOCESAIN_2027.pdf` absent du dépôt (PROMPT 2)** — Vérifié (recherche exhaustive) : ce PDF n'existe nulle part dans ce dépôt, comme `NOMINATIONS_SERVICES_COMMISSIONS_AUMONERIES_2027.pdf` avant lui. `inc/import/data-calendrier.php` reste donc un tableau vide (aucune activité/date/lieu inventé, conforme à `DECISIONS.md`) — mais `dz_import_run_calendrier()` est entièrement implémentée et testée (14 assertions via un script PHP autonome contre des données synthétiques représentatives : correspondance paroisse, lieu inconnu en texte libre, type "Agenda de l'évêque", idempotence sur second passage, échec de création). **Reste à faire dès que le PDF est fourni** : transcrire chaque ligne dans `inc/import/data-calendrier.php` (règles de transcription déjà documentées en tête de ce fichier — dates complètes, plages, heures par défaut), puis cliquer "Importer le calendrier" dans Réglages > Import contenu diocèse et vérifier en conditions réelles que les paroisses citées sont bien reliées (pas seulement en texte libre) et que l'agenda public/l'admin affichent les bons événements.
- **[2026-09-07] `NOMINATIONS_SERVICES_COMMISSIONS_AUMONERIES_2027.pdf` absent du dépôt — responsable/membres/aumôniers non transcrits (PROMPT 3)** — Vérifié (recherche exhaustive) : ce PDF n'existe toujours pas dans ce dépôt. `inc/import/data-nominations.php` contient donc les 33 entités réelles des 4 CPT couverts (noms déjà documentés dans `CONTENT_PROMPTS.md`/`SPEC.md` §3, pas inventés), mais **aucun `responsable`/`membres` réel** — chaque poste importé reste en statut `draft` pour ne rien publier d'incomplet (SPEC.md §9). `dz_import_run_nominations()` et ses 4 fonctions par CPT sont entièrement implémentées et testées (script PHP autonome, import complet des 33 entités + sous-structures de l'Économat + idempotence sur second passage). **Reste à faire dès que le PDF est fourni** : compléter `responsable`/`membres` par entité, et pour `mouvement` le champ `aumonier` (voir point suivant).
- **[2026-09-07] `mouvement_aumonier` n'a pas d'équivalent texte libre (PROMPT 3, point 3)** — Le prompt demande explicitement : relier `aumonier` à la fiche `pretre` correspondante si elle existe, "sinon laisse le champ texte simple". Or `field_dz_mouvement_aumonier` (`acf-json/group_dz_cpt_mouvement.json`) est un `post_object` (relation) sans second champ texte pour un aumônier n'ayant pas encore de fiche `pretre` — ce cas n'avait pas été anticipé quand ce champ a été créé (PROMPT 14 de `DESIGN_PROMPTS.md`). `dz_import_run_nominations_mouvement()` gère ce cas en ne renseignant rien sur la relation (plutôt que d'échouer) et en ajoutant une note explicite listant le nom de l'aumônier non trouvé — mais **aucune donnée d'aumônier réelle n'est encore transcrite** (PDF absent), donc ce chemin de code n'a pas encore été exercé en pratique. **À corriger si le besoin se confirme une fois le PDF disponible** : soit créer la fiche `pretre` manquante avant de relancer l'import, soit ajouter un second champ texte libre au groupe ACF (ex. `mouvement_aumonier_texte`) pour les aumôniers sans fiche.
- **[2026-09-07] Sous-structures de l'Économat : deux listes différentes selon la source** — `SPEC.md` §3 documente "Hôtel Carabane, Librairie Djibékel, Imprimerie du Sud" comme sous-structures de l'Économat (résumé antérieur, probablement simplifié) ; `CONTENT_PROMPTS.md` PROMPT 3 (plus proche du texte réel de la circulaire) en donne une liste différente — 4 comités ("Conseil d'Administration des Domaines agricoles", "des Unités de Production", "des Instituts d'Enseignement Supérieur", "Comité d'étude et de suivi des projets"). `inc/import/data-nominations.php` utilise exclusivement la liste de PROMPT 3, la plus directement issue de la circulaire. **À réconcilier une fois `NOMINATIONS_SERVICES_COMMISSIONS_AUMONERIES_2027.pdf` disponible** : vérifier si les deux listes coexistent (comités administratifs **et** structures opérationnelles rattachées) ou si l'une des deux est obsolète.

---

# ROADMAP (idées / améliorations futures)

Idées identifiées pendant le cadrage, volontairement hors périmètre de la v1 (voir `SPEC.md` §8 Extensibilité et `DECISIONS.md` pour le raisonnement) :

- **Dons en ligne** : intégration d'un prestataire de paiement (Orange Money, Wave, ou Stripe/PayPal selon la cible des donateurs — diaspora vs. local)
- **Multilingue** : ajout du wolof, du diola et/ou du portugais via WPML ou Polylang, selon les besoins pastoraux transfrontaliers (proximité Guinée-Bissau)
- **Calendrier liturgique / événements récurrents** : évaluer un plugin dédié (ex. The Events Calendar) si le CPT `evenement` simple devient limitant
- **Newsletter** : intégration Mailchimp/Brevo pour les actualités diocésaines
- **Espace intranet clergé** : zone réservée aux prêtres/secrétariat (hors périmètre site public)
- **Application mobile ou widget externe** : nécessiterait d'exposer les CPT via la WP REST API (voir `SPEC.md` §7 — l'architecture actuelle ne ferme pas cette porte)
- **Recherche géographique de paroisses** : carte interactive avec géolocalisation, si le nombre de paroisses le justifie
- **Cartographie du diocèse enrichie** : `page-cartographie.php` (PROMPT 16) n'affiche pour l'instant qu'un simple embed Google Maps de l'adresse générale du diocèse, en attendant des données géographiques plus précises (SPEC.md §3) — à terme, une carte affichant un marqueur par paroisse (en s'appuyant sur `paroisse_localisation`, déjà saisi par paroisse, voir `acf-json/group_dz_cpt_paroisse.json`)
- **Un seul `<h1>` par page (accessibilité/SEO)** : constaté pendant la revue PROMPT 12 — quand aucun logo image n'est configuré sur "Réglages du thème", `header.php` retombe sur `<h1 class="sitename">{nom du site}</h1>` (voir `inc/theme-setup.php`/Tâche 2), ce qui crée un second `<h1>` sur toute page utilisant déjà `template-parts/page-title.php` (ou le hero de `single.php`) pour son propre titre. Comportement hérité du template source (`starter-page.html`/`404.html` font pareil), pas une régression introduite par la conversion WordPress — non corrigé ici car hors périmètre de "chaque nouveau gabarit créé" (le fichier concerné est `header.php`, Tâche 2) ; à corriger un jour en changeant le fallback logo pour une balise non porteuse de titre (`<span>`/`<div>`), avec mise à jour du sélecteur CSS `.header .logo h1` associé
